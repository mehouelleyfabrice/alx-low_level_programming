#include <ctype.h>
#include "main.h"
/**
 * _abs - get absolute value of an integer
 * @c: the character to be checked
 * With function abs() include in c,
 * _abs computes the absolute value of an integer
 * Return: A absolute value of an integer
 */
int _abs(int c)
{
return (abs(c));
}

#include "main.h"
/**
 * add - adds two integers and returns the result
 * @a: the number to add
 * @b: the number to add
 * Return: int - Result of addition
 */
int add(int a, int b)
{
return (a + b);
}

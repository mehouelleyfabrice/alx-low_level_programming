#include "main.h"

/**
 * _strcat - that concatenates two strings
 * @dest: first string
 * @src: second string
 * Return: char*
 */

char *_strcat(char *dest, char *src)
{
	strcat(dest, src);
	return (dest);
}

#include <stdio.h>
/**
 * main - Entry point
 * Display all single digit numbers of base 10
 * Return: Always 0 (Success)
 */
int main(void)
{
int i = 48;

for (; i <= 57; i++)
	putchar(i);
putchar('\n');
return (0);
}
